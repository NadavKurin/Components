using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Components
{
    class Program
    {
        static void Main(string[] args)
        {

            ////This is an example of a testing code that you should run for all the gates that you create

            ////Create a gate
            //AndGate and = new AndGate();
            //Console.WriteLine(and + "");
            ////Test that the unit testing works properly
            //if (!and.TestGate())
            //    Console.WriteLine("bugbug");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (and.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;

            ////Test Or gate
            //OrGate or = new OrGate();
            //Console.WriteLine(or + "");
            ////Test that the unit testing works properly
            //if (!or.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (or.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;


            ////Test Xor gate
            //XorGate xor = new XorGate();
            //Console.WriteLine(xor + "");
            ////Test that the unit testing works properly
            //if (!xor.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (xor.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;

            ////Test Mux gate
            //MuxGate mux = new MuxGate();
            //Console.WriteLine(mux + "");
            ////Test that the unit testing works properly
            //if (!mux.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (mux.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;

            ////Test DeMux gate
            //Demux demux = new Demux();
            //Console.WriteLine(demux + "");
            ////Test that the unit testing works properly
            //if (!demux.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (demux.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;


            ////Test multiBitAndGate gate
            //MultiBitAndGate multiBitAndGate = new MultiBitAndGate(3);
            //Console.WriteLine(multiBitAndGate + "");
            ////Test that the unit testing works properly
            //if (!multiBitAndGate.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (multiBitAndGate.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;

            ////Test multiBitOrGate gate
            //MultiBitOrGate multiBitOrGate = new MultiBitOrGate(4);
            //Console.WriteLine(multiBitOrGate + "");
            ////Test that the unit testing works properly
            //if (!multiBitOrGate.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (multiBitOrGate.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;


            ////Test BitwiseAndGate gate
            //BitwiseAndGate bitwiseAndGate = new BitwiseAndGate(4);
            //Console.WriteLine(bitwiseAndGate + "");
            ////Test that the unit testing works properly
            //if (!bitwiseAndGate.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (bitwiseAndGate.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;



            ////Test BitwiseNotGate gate
            //BitwiseNotGate bitwiseNotGate = new BitwiseNotGate(4);
            //Console.WriteLine(bitwiseNotGate + "");
            ////Test that the unit testing works properly
            //if (!bitwiseNotGate.TestGate())
            //    Console.WriteLine("bugbug1");



            ////Test BitwiseOrGate gate
            //BitwiseOrGate bitwiseOrGate = new BitwiseOrGate(4);
            //Console.WriteLine(bitwiseOrGate + "");
            ////Test that the unit testing works properly
            //if (!bitwiseOrGate.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (bitwiseOrGate.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;



            ////Test BitWiseMuxGate gate
            //BitwiseMux bitwiseMux = new BitwiseMux(4);
            //Console.WriteLine(bitwiseMux + "");
            ////Test that the unit testing works properly
            //if (!bitwiseMux.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (bitwiseMux.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;


            ////Test BitWiseDeMuxGate gate
            //BitwiseDemux bitwiseDeMux = new BitwiseDemux(4);
            //Console.WriteLine(bitwiseDeMux + "");
            ////Test that the unit testing works properly
            //if (!bitwiseDeMux.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (bitwiseDeMux.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;


            ////Test HaldAdder gate
            //HalfAdder halfAdder = new HalfAdder();
            //Console.WriteLine(halfAdder + "");
            ////Test that the unit testing works properly
            //if (!halfAdder.TestGate())
            //    Console.WriteLine("bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (halfAdder.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;

            ////Test FullAdder gate
            //FullAdder fullAdder = new FullAdder();
            //Console.WriteLine(fullAdder + "");
            ////Test that the unit testing works properly
            //if (!fullAdder.TestGate())
            //    Console.WriteLine("Fulladder bugbug1");

            ////Now we ruin the nand gates that are used in all other gates. The gate should not work properly after this.
            //NAndGate.Corrupt = true;
            //if (fullAdder.TestGate())
            //    Console.WriteLine("bugbug");
            //NAndGate.Corrupt = false;

            ////Test BitwiseMultiwayMux gate
            //BitwiseMultiwayMux bitwiseMultiwayMux = new BitwiseMultiwayMux(7, 3);
            //if (!bitwiseMultiwayMux.TestGate())
            //    Console.WriteLine("bugbug1");


            ////Test BitwiseMultiwayDeMux gate
            //BitwiseMultiwayDemux bitwiseMultiwayDemux = new BitwiseMultiwayDemux(4, 4);
            ////Test that the unit testing works properly
            //if (!bitwiseMultiwayDemux.TestGate())
            //    Console.WriteLine("bugbug1");


            //WireSet w = new WireSet(4);
            //if(!w.testSet2sComp())
            //    Console.WriteLine("bugbug1");

            //Console.WriteLine("done");
            //Console.ReadLine();
            ALU alue = new ALU(2);

        }    

    }
}
